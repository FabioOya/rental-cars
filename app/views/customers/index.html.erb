<h1>Clientes</h1>
<% if Customer.count == 0 %>
    <p> Nenhum cliente cadastrado <p>
<% end %>
<ul>
  <% @customers.each do |c| %>
    <li><%= link_to c.name, customer_path(id: c.id) %></li>
  <% end %>
</ul>
<%= link_to 'Voltar', root_path %>
<%= link_to 'Registrar novo cliente', new_customer_path %>
